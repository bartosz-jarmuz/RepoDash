<UserControl x:Class="RepoDash.App.Controls.GlobalGitOperationsMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:RepoDash.App.ViewModels"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:GlobalGitOperationsMenuViewModel}">
  <Menu VerticalAlignment="Center">
    <MenuItem Header="Git">
      <MenuItem Header="Fetch all" Command="{Binding GitFetchAllCommand}"/>
      <MenuItem Header="Pull all (merge)" Command="{Binding GitPullAllCommand}"/>
      <MenuItem Header="Pull all (rebase)" Command="{Binding GitPullRebaseAllCommand}"/>
    </MenuItem>
  </Menu>
</UserControl>