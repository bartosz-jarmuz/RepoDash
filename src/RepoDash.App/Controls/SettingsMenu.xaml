<UserControl x:Class="RepoDash.App.Controls.SettingsMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:RepoDash.App.ViewModels"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:SettingsMenuViewModel}">
  <Menu VerticalAlignment="Center">
    <MenuItem Header="Settings">
      <MenuItem Header="General" Command="{Binding OpenGeneralSettingsCommand}" />
      <MenuItem Header="Repositories" Command="{Binding OpenRepositoriesSettingsCommand}" />
      <MenuItem Header="Shortcuts" Command="{Binding OpenShortcutsSettingsCommand}" />
      <MenuItem Header="Colors" Command="{Binding OpenColorSettingsCommand}" />
      <MenuItem Header="External Tools" Command="{Binding OpenExternalToolsSettingsCommand}" />
    </MenuItem>
  </Menu>
</UserControl>