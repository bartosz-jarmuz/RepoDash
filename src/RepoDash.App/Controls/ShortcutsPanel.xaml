<UserControl x:Class="RepoDash.App.Controls.ShortcutsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:RepoDash.App.Controls"
             xmlns:primitives="clr-namespace:System.Windows.Controls.Primitives;assembly=PresentationFramework"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=UserControl}"
             Loaded="OnLoaded"
             Unloaded="OnUnloaded">
  <UserControl.Resources>
    <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
    <Style x:Key="PanelResizeThumbStyle" TargetType="primitives:Thumb">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="primitives:Thumb">
            <Border Background="{TemplateBinding Background}">
              <Border HorizontalAlignment="Center"
                      Width="1"
                      Margin="2"
                      CornerRadius="5"
                      Background="LightGray"/>
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </UserControl.Resources>

  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>

    <Border x:Name="PanelBorder"
            Grid.Column="0"
            Padding="4"
            Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
            CornerRadius="6"
            SnapsToDevicePixels="True"
            ClipToBounds="True">
      <ScrollViewer HorizontalScrollBarVisibility="Auto"
                    VerticalScrollBarVisibility="Auto">
        <StackPanel Orientation="{Binding ItemsOrientation, RelativeSource={RelativeSource AncestorType=UserControl}}">
          <ItemsControl x:Name="Items"
                        Margin="0"
                        AllowDrop="True"
                        Background="Transparent"
                        ItemsSource="{Binding DisplayItems, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        PreviewDragOver="OnItemsPreviewDragOver"
                        DragLeave="OnItemsDragLeave"
                        Drop="OnItemsDrop">
            <ItemsControl.ItemsPanel>
              <ItemsPanelTemplate>
                <StackPanel Orientation="{Binding ItemsOrientation, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            Margin="2"/>
              </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <Button Padding="4"
                        Margin="2"
                        ToolTip="{Binding DisplayName}"
                        Click="OnShortcutClick"
                        PreviewMouseLeftButtonDown="OnShortcutPreviewMouseLeftButtonDown"
                        PreviewMouseMove="OnShortcutPreviewMouseMove">
                  <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                    <Grid Width="{Binding IconPixelSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                          Height="{Binding IconPixelSize, RelativeSource={RelativeSource AncestorType=UserControl}}">
                      <Image Source="{Binding Icon}" Stretch="Uniform"
                             Visibility="{Binding Icon, Converter={x:Static controls:VisibilityConverters.FromNullToCollapsed}}"/>
                      <Border Background="#22000000" CornerRadius="4" Padding="0"
                              Visibility="{Binding Icon, Converter={x:Static controls:VisibilityConverters.FromNotNullToCollapsed}}">
                        <TextBlock Text="{Binding FallbackGlyph}"
                                   FontSize="{Binding GlyphFontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                      </Border>
                    </Grid>
                    <TextBlock Text="{Binding DisplayName}"
                               Margin="0,4,0,0"
                               FontSize="{Binding LabelFontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                               TextTrimming="CharacterEllipsis"
                               HorizontalAlignment="Center"
                               MaxWidth="{Binding MaxLabelWidth, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                  </StackPanel>
                  <Button.ContextMenu>
                    <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                      <MenuItem Header="Rename..." Click="OnRenameShortcutClick" CommandParameter="{Binding}"/>
                      <MenuItem Header="Delete" Click="OnDeleteShortcutClick" CommandParameter="{Binding}"/>
                    </ContextMenu>
                  </Button.ContextMenu>
                </Button>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
          <Button x:Name="AddButton"
                  Margin="2"
                  Padding="8"
                  Click="OnAddShortcutClick">
            <Button.ToolTip>
              <ToolTip>Add shortcut</ToolTip>
            </Button.ToolTip>
            <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
              <Border Width="{Binding IconPixelSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                      Height="{Binding IconPixelSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                      Background="#11000000"
                      BorderBrush="#44000000"
                      BorderThickness="1"
                      CornerRadius="4">
                <TextBlock Text="+"
                           FontSize="{Binding GlyphFontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
              </Border>
              <TextBlock Text="Add"
                         Margin="0,4,0,0"
                         FontWeight="SemiBold"
                         FontSize="{Binding LabelFontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                         HorizontalAlignment="Center"/>
            </StackPanel>
          </Button>
        </StackPanel>
      </ScrollViewer>
    </Border>

    <primitives:Thumb x:Name="ResizeThumb"
                      Grid.Column="1"
                      Width="8"
                      VerticalAlignment="Stretch"
                      Cursor="SizeWE"
                      Style="{StaticResource PanelResizeThumbStyle}"
                      DragDelta="OnResizeThumbDragDelta"
                      DragCompleted="OnResizeThumbDragCompleted"
                      Visibility="{Binding IsResizeEnabled, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVisibility}}"/>
  </Grid>
</UserControl>
