<UserControl x:Class="RepoDash.App.Controls.RepoItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:RepoDash.App.ViewModels"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:RepoDash.App.Controls"
             xmlns:converters="clr-namespace:RepoDash.App.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:RepoItemViewModel}">
  <UserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/RepoDash.App;component/Themes/Colors.xaml"/>
        <ResourceDictionary Source="/RepoDash.App;component/Themes/Styles.xaml"/>
      </ResourceDictionary.MergedDictionaries>
      <converters:StoryReferenceToBrushConverter x:Key="StoryReferenceBackgroundConverter" Mode="Background"/>
      <converters:StoryReferenceToBrushConverter x:Key="StoryReferenceForegroundConverter" Mode="Foreground"/>
    </ResourceDictionary>
  </UserControl.Resources>

  <Grid Margin="0,4"
        ToolTip="{Binding Path, Mode=OneTime}"
        Background="Transparent"
        MouseLeftButtonDown="OnRootMouseLeftButtonDown">
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
      <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>

    <!-- Left: title + tiny branch (+ optional pin icon) -->
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>

      <TextBlock Grid.Column="0"
                 Text="📌"
                 FontSize="14"
                 Margin="0,0,6,0"
                 VerticalAlignment="Center">
        <TextBlock.Style>
          <Style TargetType="TextBlock">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
              <DataTrigger Binding="{Binding IsPinned}" Value="True">
                <Setter Property="Visibility" Value="Visible"/>
              </DataTrigger>
              <DataTrigger Binding="{Binding AllowPinning, RelativeSource={RelativeSource AncestorType=controls:RepoItem}}" Value="False">
                <Setter Property="Visibility" Value="Collapsed"/>
              </DataTrigger>
            </Style.Triggers>
          </Style>
        </TextBlock.Style>
      </TextBlock>

      <StackPanel Grid.Column="1">
        <StackPanel Orientation="Horizontal">
          <TextBlock>
            <Run Text="{Binding Name}" FontWeight="SemiBold"/>
          </TextBlock>
          <Button FontSize="10"
                  Padding="6,0"
                  Margin="5,0"
                  BorderThickness="0"
                  Command="{Binding OpenStoryCommand}"
                  Visibility="{Binding HasStoryLink, Converter={StaticResource BooleanToVisibilityConverter}}"
                  ToolTip="Open Story"
                  FontWeight="Medium"
                  Background="{Binding StoryReference, Converter={StaticResource StoryReferenceBackgroundConverter}}"
                  BorderBrush="{Binding StoryReference, Converter={StaticResource StoryReferenceBackgroundConverter}}">
            <TextBlock Text="{Binding StoryReference}"
                       Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
          </Button>
        </StackPanel>
        <TextBlock FontSize="10" Margin="0,2,0,0" FontWeight="SemiBold">
          <TextBlock.Style>
            <Style TargetType="TextBlock">
              <Setter Property="Foreground" Value="{StaticResource BrushTextSubtle}"/>
              <Style.Triggers>
                <DataTrigger Binding="{Binding SyncState}" Value="UpToDate">
                  <Setter Property="Foreground" Value="Green"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding SyncState}" Value="Ahead">
                  <Setter Property="Foreground" Value="DodgerBlue"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding SyncState}" Value="Behind">
                  <Setter Property="Foreground" Value="DarkOrange"/>
                </DataTrigger>
              </Style.Triggers>
            </Style>
          </TextBlock.Style>
          <TextBlock Text="*" Foreground="Red" FontWeight="Black"
                     Visibility="{Binding IsDirty, Converter={StaticResource BooleanToVisibilityConverter}}"
                     FontSize="10" Margin="0" Padding="0"/>
          <Hyperlink ToolTip="Click to open remote"
                     Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=TextBlock}}"
                     Command="{Binding DataContext.OpenRemoteCommand, RelativeSource={RelativeSource AncestorType=TextBlock}}"
                     CommandParameter="{Binding CurrentBranch}">
            <Run Text="{Binding CurrentBranch}"/>
          </Hyperlink>
        </TextBlock>
      </StackPanel>
    </Grid>
    <UniformGrid Grid.Column="1" HorizontalAlignment="Right" Rows="2">
      <Button Style="{StaticResource IconFlatButton}" Command="{Binding BrowseCommand}" ToolTip="Browse" Margin="0,0,4,0">
        <TextBlock Text="📁"/>
      </Button>
      <Button Style="{StaticResource IconFlatButton}" Command="{Binding OpenPipelinesCommand}" ToolTip="Pipelines" Margin="0,0,4,0">
        <TextBlock Text="🛳️"/>
      </Button>
      <Button Style="{StaticResource IconFlatButton}" Command="{Binding OpenGitCliCommand}" ToolTip="Git Command line" Margin="0,0,4,0">
        <TextBlock Text="⌨️"/>
      </Button>
      <Button Style="{StaticResource IconFlatButton}" Command="{Binding OpenGitUiCommand}" ToolTip="Git UI" Margin="0,0,4,0">
        <TextBlock Text="👷‍"/>
      </Button>
      <Button Style="{StaticResource IconFlatButton}" Command="{Binding CopyNameCommand}" ToolTip="Copy name" Margin="0,0,4,0">
        <TextBlock Text="⎘"/>
      </Button>
      <Button Style="{StaticResource IconFlatButton}" Command="{Binding CopyPathCommand}" ToolTip="Copy path" Margin="0,0,4,0">
        <TextBlock Text="📋"/>
      </Button>
    </UniformGrid>


    <!-- Context menu -->
    <Grid.ContextMenu>
      <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
        <MenuItem Header="Launch" Command="{Binding LaunchCommand}"/>
        <MenuItem Header="Browse" Command="{Binding BrowseCommand}"/>
        <Separator/>
        <MenuItem Header="Open Remote" Command="{Binding OpenRemoteCommand}"/>
        <MenuItem Header="Open Pipelines" Command="{Binding OpenPipelinesCommand}"/>
        <MenuItem Header="Git UI" Command="{Binding OpenGitUiCommand}"/>
        <MenuItem Header="Git Command Line" Command="{Binding OpenGitCliCommand}"/>
        <Separator/>
        <MenuItem Header="Copy Name" Command="{Binding CopyNameCommand}"/>
        <MenuItem Header="Copy Path" Command="{Binding CopyPathCommand}"/>
        <Separator/>
        <MenuItem Header="Pin" Command="{Binding TogglePinCommand}" IsCheckable="True" IsChecked="{Binding IsPinned, Mode=OneWay}"/>
        <MenuItem Header="Hide from recent/frequent" Command="{Binding ToggleBlacklistCommand}"/>
      </ContextMenu>
    </Grid.ContextMenu>
  </Grid>
</UserControl>
