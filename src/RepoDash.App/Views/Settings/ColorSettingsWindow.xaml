<Window x:Class="RepoDash.App.Views.Settings.ColorSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        Title="Color Rules" Width="760" Height="540" WindowStartupLocation="CenterOwner"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:vm="clr-namespace:RepoDash.App.ViewModels.Settings"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance vm:ColorSettingsViewModel}"
        >
  <DockPanel Margin="10">
    <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
      <Button Command="{Binding SaveCommand}" Width="90" Margin="0,0,8,0">Save</Button>
      <Button Command="{Binding CancelCommand}" Width="90">Cancel</Button>
    </StackPanel>

    <xctk:PropertyGrid SelectedObject="{Binding}"
                       AutoGenerateProperties="False"
                       >
      <xctk:PropertyGrid.PropertyDefinitions>
        <xctk:PropertyDefinition TargetProperties="Rules" DisplayName="Rules" Description="Keyword-based colorization rules."/>
      </xctk:PropertyGrid.PropertyDefinitions>
    </xctk:PropertyGrid>
  </DockPanel>
</Window>