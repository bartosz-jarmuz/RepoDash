<Window x:Class="RepoDash.App.Views.BlacklistedItemsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:RepoDash.App.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance vm:BlacklistedItemsViewModel}"
        Title="Manage Blacklisted Repositories"
        Width="640"
        Height="720"
        WindowStartupLocation="CenterOwner">
  <Window.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/RepoDash.App;component/Themes/Colors.xaml"/>
        <ResourceDictionary Source="/RepoDash.App;component/Themes/Styles.xaml"/>
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </Window.Resources>

  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock Text="Repositories currently hidden via blacklist." FontWeight="SemiBold"/>

    <Grid Grid.Row="1" Margin="0,12,0,12">
      <ListView ItemsSource="{Binding Items}"
                Visibility="{Binding HasItems, Converter={StaticResource BooleanToVisibilityConverter}}">
        <ListView.ItemTemplate>
          <DataTemplate>
            <Grid Margin="0,2">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
              </Grid.ColumnDefinitions>
              <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
              <TextBlock Grid.Column="1"
                         Margin="12,0,0,0"
                         Text="{Binding Path}"
                         TextTrimming="CharacterEllipsis"/>
              <Button Grid.Column="2"
                      Content="Restore"
                      Command="{Binding RestoreCommand}"
                      Margin="12,0,0,0"
                      Padding="12,4"/>
            </Grid>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
      <TextBlock Text="No blacklisted repositories."
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center"
                 Foreground="{StaticResource BrushTextSubtle}"
                 FontStyle="Italic"
                 Visibility="{Binding HasNoItems, Converter={StaticResource BooleanToVisibilityConverter}}"/>
    </Grid>

    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="Close" IsCancel="True" Padding="16,4" Width="100"/>
    </StackPanel>
  </Grid>
</Window>
